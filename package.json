{
  "name": "@how-much-do-i-make/app",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
  	"build": "lerna run build",
  	"start": "lerna run --parallel start"
  }
}
